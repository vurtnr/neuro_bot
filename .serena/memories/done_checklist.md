Before considering a NeuroBot task complete: rebuild the affected package(s) in ~/neuro_bot_ws, rebuild robot_interfaces first if interfaces changed, run the most relevant Python tests for modified packages, and do at least one ROS runtime validation using launch scripts, node startup, topics, or services. Be careful with start.sh because it copies generated shared libraries into /usr/lib and runs sudo ldconfig.